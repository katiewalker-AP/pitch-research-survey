# Google Apps Script Web App URL that accepts JSON POST requests.
# Not prefixed with NEXT_PUBLIC — this is read server-side only (never exposed to the browser).
# Replace with your actual deployed Apps Script URL.
FORM_ENDPOINT=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
